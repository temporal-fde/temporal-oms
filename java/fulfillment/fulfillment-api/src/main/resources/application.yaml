spring.config.import: classpath:acme.fulfillment.yaml
spring.application.name: fulfillment-api
spring.temporal.start-workers: false

management.server.port: 9091

server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs
  show-actuator: false
