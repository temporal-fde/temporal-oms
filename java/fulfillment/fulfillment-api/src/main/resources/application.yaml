spring.config.import: classpath:acme.fulfillment.yaml
spring.profiles.default: local
spring.application.name: fulfillment-api
spring.temporal.start-workers: false

management.server.port: 9071

server.port: 8060

easypost:
  api-key: ${EASYPOST_API_KEY}
  warehouse:
    from-address-id: ${EASYPOST_WAREHOUSE_FROM_ADDRESS_ID:}

springdoc:
  api-docs:
    path: /v3/api-docs
  show-actuator: false
